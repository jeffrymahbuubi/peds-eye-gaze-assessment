# SPEC-2026-09-02 — Physician Feedback Triage

**Status:** mixed — diagnosis for all 7 items, **implementation done for items 1, 2, 3, 4, 5, and 6** (2026-09-03/2026-09-04, see "Implementation status" below; item 4's playback is not yet audibly confirmed). **Item 7 is now fully implemented: both Goal 1 (`--calibration-file` reuse) and Goal 2 (1-9 point calibration, previously locked to exactly 5 or 9) shipped 2026-09-04** (see item 7 below) — currently uncommitted, by the user's own choice. Goal 2's actual accuracy tradeoff at low point counts is still unmeasured and needs the real device; the capability itself is what was asked for, specifically so the user can measure and report that tradeoff to the physician later. The user's stated priority: finish backend/data-pipeline work before starting the planned full GUI.

**Scope note:** all "investigated" claims below come from reading the current working copy `dev/peds-eye-gaze-assessment/` on 2026-09-02. Domain/clinical claims are cited from external sources found via web research (parallel-search MCP). The "Implemented"/"Partially implemented" notes added under items 3, 4, and 6 (2026-09-03) are the exception — those changes were made in the codebase; everything else in this document remains diagnosis-only, not yet implemented.

## Implementation status (updated 2026-09-03)

The desktop had no GP3HD connected on 2026-09-03, so the device-independent
subset of these 7 items was implemented first (full triage of which items
need the device: see the `peds-eye-gaze-assessment-device-independent-tasks-2026-09-03`
memory).

| # | Item | Status |
|---|---|---|
| 1 | Gaze jitter / progress ring | **Implemented 2026-09-04** — see item 1 below |
| 2 | Gaze-on-target instant feedback | **Implemented 2026-09-04** — see item 2 below |
| 3 | Multi-item task visuals (click_grid/scanning) | **Implemented 2026-09-03** — see item 3 below |
| 4 | Sound feedback | **Implemented 2026-09-03** — see item 4 below. Playback not yet audibly confirmed (session-local audio-device limitation, not a code/asset defect) — awaiting user listen-test |
| 5 | Speed/pacing config documentation | **Implemented 2026-09-04** — see item 5 below (README.md) |
| 6 | Session data fixation/saccade metrics | **Partially implemented 2026-09-03** — see item 6 below |
| 7 | Calibrate-once-per-sitting | **Both goals implemented 2026-09-04.** Goal 1: `--calibration-file` reuse. Goal 2: calibration.points now accepts 1-9 (was locked to 5 or 9) — see item 7 below. Accuracy at low point counts still needs live-device measurement. |

**Update 2026-09-04 — items 1, 2, and 5 implemented (still no GP3HD connected;
all three built and tested against the fake-server/replay-fixture pattern,
per the device-independent-tasks triage):**

- **Item 1 fixed:** `GazeSmoother` (new, `src/inputs/eye_input.py`) applies an
  exponential moving average to the raw point-of-gaze before it becomes the
  `Pointer` used for cursor rendering and on-target hit-testing —
  deliberately *not* to the raw `GazeSample` handed to the recorder, so
  `gaze_stream.csv` and its fixation/saccade metrics
  (`CLINICAL_DATA_REFERENCE.md`) stay untouched. Configurable via new
  `dwell.smoothing.enabled`/`dwell.smoothing.alpha` in `configs/default.yaml`
  (default `alpha: 0.35`). Wired into `EyeInput` (`src/inputs/eye_input.py`)
  and `AssessmentApp.__init__` (`src/app.py`).
- **Item 2 fixed:** `BaseTask.update` (`src/tasks/base_task.py`) now reports
  an instant `FrameResult.on_target` flag (true the moment gaze enters the
  hitbox, independent of dwell accumulation). `TaskCanvas` (new
  `_draw_instant_feedback`, `src/ui/canvas.py`) draws a full-brightness ring
  around the target immediately when `on_target` is true, distinct from the
  dwell progress ring (which still only fills over `threshold_ms`). New
  `dwell.instant_feedback` toggle in `configs/default.yaml` (default `true`).
  Visually confirmed via qt-mcp screenshot against
  `--replay tests/fixtures/gaze_replay.jsonl` — the ring appears the instant
  the replayed gaze reaches a target, well before the progress arc has grown.
- **Item 5 done:** new "Adjusting target speed and pacing between trials"
  section in `README.md` documents `motion.speed_frac_per_s`/
  `select_window_ms` (`follow_moving.yaml` only) and
  `inter_trial_interval_ms`/`timeout_ms` (every task), aimed at a
  non-engineer (therapist/RA) reader per this project's existing
  YAML-editing conventions.
- **Regression test for the shared 1/2 root cause**
  (`tests/test_eye_input.py::test_smoothing_lets_dwell_complete_despite_jitter_that_defeats_raw_pointer`):
  drives a deterministic square-wave gaze wobble (150ms half-period, 15px
  past the hitbox edge — chosen so raw jitter exceeds the 120ms hold-grace on
  every swing) through `EyeInput`, and confirms dwell never completes with
  smoothing off but reliably completes with it on. Additional unit tests:
  `tests/test_gaze_smoothing.py` (EMA variance reduction, disabled
  passthrough, reset-on-dropout) and
  `tests/test_task_pipeline.py::test_frame_result_reports_on_target_instantly_not_gated_by_dwell`.
  80 tests total, pytest all headless except the one pre-existing,
  unrelated `target_fps` config-vs-test mismatch noted below.
- **Known caveat, found while testing, NOT fixed here (out of this task's
  scope):** `tests/test_task_pipeline.py::test_config_merges_task_over_default`
  asserts `cfg["app"]["target_fps"] == 60`, but `configs/default.yaml` has
  shipped `target_fps: 150` since the initial commit (`git log -p` shows no
  commit ever set it to 60) — this is a stale test expectation, not a
  skip-worktree local-edit artifact as an earlier session's memory assumed.
  Left alone since it's unrelated to items 1/2/5; worth a one-line fix
  (update the assertion to 150, or the config to 60, whichever is actually
  intended) in a future session.
- **Not yet done for item 1:** smoothing's `alpha=0.35` default is a
  reasonable middle ground (responsive enough for `follow_moving`, smooth
  enough for the tested jitter pattern above) but has not been tuned against
  *real* GP3HD noise characteristics — that requires the live device and
  should be revisited once it's reconnected.
- **Significant bonus finding while qt-mcp-verifying item 2, fixed the same
  session:** `TaskCanvas._draw_target`'s pre-existing "selectable" outline
  ring and `_draw_progress_ring`'s dwell-fill arc (`src/ui/canvas.py`) had
  never actually been rendering, in any prior session, for any task. Root
  cause: both built their pen via `pen = painter.pen(); pen.setColor(...);
  pen.setWidth(...); painter.setPen(pen)` — but the painter's *current* pen
  at that point is `Qt.PenStyle.NoPen` (just set for the target's gradient
  fill moments earlier), and mutating a `NoPen` pen's color/width does not
  change its style away from `NoPen`, so `drawEllipse`/`drawArc` silently
  drew nothing. My new `_draw_instant_feedback` copied the same pattern and
  had the identical bug, which is what caught it — a live qt-mcp screenshot
  of a paused, on-target frame showed no rings at all where the target was.
  Fixed all three (plus `_draw_layout_slots`, defensively, though it wasn't
  actually broken since it runs before anything sets `NoPen`) by
  constructing a fresh `QPen(color, width)` instead of mutating
  `painter.pen()`. **This retroactively explains item 1's own diagnosis
  note** ("Toggling `dwell.progress_ring` produced no visible change") as
  likely this rendering bug, not only the "ring only paints once
  dwell_progress > 0" theory written at triage time — the ring would have
  stayed invisible even when dwell *did* accumulate. Visually confirmed
  fixed via qt-mcp: a paused, on-target `click_static` frame now shows the
  thin full instant-feedback ring and the thicker partial dwell-progress arc
  as two clearly distinct rings.

**Update (same day, later):** item 6 below is now superseded by a dedicated, more detailed evergreen doc — see [`dev/peds-eye-gaze-assessment/docs/CLINICAL_DATA_REFERENCE.md`](../../dev/peds-eye-gaze-assessment/docs/CLINICAL_DATA_REFERENCE.md), which also answers a follow-up question this document didn't cover: whether Gazepoint Analysis's own session-level/AOI summaries are API-accessible (they are not — see that doc's "Gazepoint Analysis's own session-level summary" section). Item 6 is left below unchanged as a historical record of what was known at triage time; treat `CLINICAL_DATA_REFERENCE.md` as current.

## Context

The user (PI's collaborator) met with a physician who tested the assessment tool with `follow_along` and the other three tasks. This raised several questions and one usability complaint about jitter. Goal of this session: understand and diagnose each item, not fix it.

---

## 1. Visual cursor jitter, the "progress ring" toggle, and distraction risk

**Observed:** in `follow_along`, `dwell.visual_cursor` toggles the yellow gaze cursor on/off as expected. Toggling `dwell.progress_ring` produced no visible change. Raw cursor jitter is distracting to children during trials (observed by the physician).

**Investigated:**
- `dwell.visual_cursor` (`configs/default.yaml`) → `TaskCanvas.show_cursor` (`src/ui/canvas.py`). Wired correctly — matches what was observed.
- `dwell.progress_ring` → `TaskCanvas.show_progress_ring`. Also wired correctly, **but** the ring (`_draw_progress_ring`) is only ever painted while `dwell_progress > 0`, i.e. only during an active dwell accumulation. If gaze never accumulated dwell progress during the test (see item 2), the ring would never have appeared regardless of the toggle — so "no visible change" doesn't necessarily mean the setting is broken. **(2026-09-04 update: it turned out to be broken too — see the "bonus finding" note in the Implementation status section above. Even when `dwell_progress` was > 0, a pen-construction bug meant the arc never actually rendered, in any session before this fix.)**
- The cursor position painted every frame comes straight from the tracker's raw best-point-of-gaze (BPOGX/BPOGY, falling back to FPOGX/FPOGY in `gazepoint_client.py::rec_to_sample`). **No smoothing, averaging, or filtering is applied anywhere in the codebase** before that point is rendered. Raw point-of-gaze is inherently noisy on any video-based tracker, and pediatric gaze tends to be noisier than adult gaze — the pipeline currently passes 100% of that raw noise straight to the screen. This is a confirmed, real gap, not a device malfunction.

**Open question:** is jitter itself the nuisance, or is it mainly a problem because it interferes with selection (item 2)? Both point to the same root cause (unfiltered raw signal) but the answer affects where a future fix should focus.

---

## 2. Gaze on target doesn't "light up" the target

**Observed:** gaze landing on the follow_along target doesn't visibly register right away; feels unresponsive.

**Investigated:**
- The target's appearance does not change the instant gaze lands on it. The *only* on-target feedback is the dwell progress ring, which requires **800ms continuous on-target dwell** (`dwell.threshold_ms`) to complete.
- A **120ms hold-grace** window (`DwellConfig.hold_grace_ms` in `src/inputs/eye_input.py`, hardcoded, not in any YAML) tolerates brief drop-offs without resetting — but if the jittery raw gaze (item 1) drifts outside the target hitbox for longer than 120ms, dwell progress resets to zero and restarts.
- There is no "instant" visual acknowledgment of on-target gaze independent of the slow-filling ring.
- **This is very likely the same underlying issue as item 1, seen from a different angle:** jittery raw gaze pushes the pointer in and out of the hitbox faster than the 120ms grace tolerates, so dwell progress keeps resetting before it becomes visually obvious — which looks and feels exactly like "gaze on target, nothing happens."
- The hitbox itself already has generous slack (`dwell.jitter_tolerance_px = 40px` added to the target radius), so positional tolerance is not the bottleneck — the 120ms *temporal* tolerance is.

---

## 3. What each of the four tasks is for, and why three of them look alike

**Observed:** `follow_along` is distinguishable, but `click_static`, `click_grid`, and `scanning` look similar during testing.

**Investigated (implementation fact, stated for diagnosis only):**
- `TaskCanvas.paintEvent` (`src/ui/canvas.py`) only ever draws **one** target circle — the single currently-active target, plus the cursor, dwell ring, and hit particles. There is no rendering path for multiple simultaneous shapes.
- `click_grid.yaml` declares a 3×3 grid and a `highlight_color`, implying "show all 9 cells, highlight the active one." `scanning.yaml` declares `n_icons: 4` distractors and its own `highlight_color`, implying "show all icons, highlight the target." **Neither layout is actually drawn.** Both tasks currently render exactly like `click_static`: one circle appears at a new position each trial. The grid/icon-slot logic exists only in the Python target-*position* sampling (`click_grid.py`, `scanning.py`) and the YAML — never on screen.
- **This is the direct cause of the "they all look the same" observation.** Visually, all three are currently indistinguishable single-target click tasks; only the underlying position-sampling pattern differs (fixed grid cells vs. 8 random candidate positions vs. a row of slots), and none of that is visible to the child or observer.

**Clinical purpose of each task (external research):**

| Task | Mechanism | What it's designed to measure | Supporting source |
|---|---|---|---|
| `follow_moving` | Target moves along a path; child must continuously track and select it in a timed window | **Smooth pursuit** (continuous tracking), a distinct oculomotor system from fixation/saccades with faster onset latency (~90-150ms vs ~200-250ms for voluntary saccades) | Wikipedia/iMotions eye-movement physiology overview |
| `click_static` | One static target at a random position; fixate + dwell-select | **Single-target fixation (STF)** — ability to direct and hold gaze on one isolated static stimulus | Matches the "STF" task in a 2025 peer-reviewed study assessing gaze fixation/shift specifically in children with CP (PubMed 40217776, *J Clin Med* 2025) |
| `click_grid` | Explicitly modeled on "Compass" (per its own code docstring); 3×3 grid, one cell should light up | **Spatial/positional selection accuracy** among an array of candidate locations — closer to real AAC grid-interface use than raw oculomotor testing | Compass is the same software used in a published longitudinal study measuring gaze-based AT performance (time-on-task, accuracy) in children with CP (PMC4867850) |
| `scanning` | Several icons shown; one is cued as target among distractors | **Visual search / target-target fixation shift (TTFS)** — finding and shifting gaze to a designated item among competitors, distinct from static fixation | Matches "TTFS," the second task type in the same 2025 CP study above; matches the general OT distinction between "visual tracking" (single moving target) and "visual scanning" (searching among several) (napacenter.org) |

**Conclusion:** the four tasks are clinically well-differentiated *by design* (pursuit vs. static fixation vs. spatial accuracy vs. search/shift), but `click_grid` and `scanning` are not currently rendering the multi-item layouts that would make that distinction visible — and, arguably, clinically valid. Right now they behave as `click_static` with different position sampling, which is exactly what was observed.

**Implemented 2026-09-03:** `BaseTask.layout_slots` (new, `src/tasks/base_task.py`) exposes each multi-item task's candidate positions; `ClickGridTask`/`ScanningTask` populate it (`src/tasks/click_grid.py`, `src/tasks/scanning.py`). `TaskCanvas._draw_layout_slots` (new, `src/ui/canvas.py`) paints every slot as a dim outline with the active target drawn bright on top, wired through `app.py`. Visually confirmed via qt-mcp screenshot against `--replay tests/fixtures/gaze_replay.jsonl` (no real device) for both tasks — `click_grid` now shows its 3x3 grid, `scanning` its 4-icon row. Tests: `test_click_grid_exposes_layout_slots_for_gui`, `test_scanning_exposes_layout_slots_for_gui`, `test_click_static_has_no_layout_slots` in `tests/test_task_pipeline.py`.

---

## 4. Missing sound feedback: bug, or missing asset?

**Observed:** no sound when a trial completes.

**Investigated — confirmed: not a bug. The feature was never built.**
- No audio files exist anywhere in the repository (searched for `.wav`/`.mp3`/`.ogg` — none found).
- No audio-playback code exists anywhere in `src/` (searched for `QSound`, `QMediaPlayer`, `playsound`, `winsound` — no matches).
- `FeedbackBus` (`src/engine/feedback.py`) is the intended abstraction for feedback effects, but the GUI's concrete implementation (`GuiFeedback` in `src/app.py`) only implements `on_hit` → a particle burst on the canvas. `on_miss`, `on_progress`, and `on_target_shown` are all no-ops — no sound is triggered for any of them.
- **Every** task YAML (`click_static.yaml`, `click_grid.yaml`, `scanning.yaml`, `follow_moving.yaml`) declares `feedback.hit_sound: true` and `feedback.miss_sound: true` — but these keys are never read by any Python code. They are dead configuration, most likely a placeholder left over from the original design plan that was never wired up.
- **Answer:** audio feedback is a planned-but-unbuilt feature, not a regression. (Informational only, not a plan: PySide6 already ships `QSoundEffect`/`QMediaPlayer` for whenever this gets built, so no new dependency would be needed — only sound assets, which would need to be sourced/licensed.)

**Implemented 2026-09-03.** Discrete hit/miss cues only (no continuous background music — deliberately out of scope, see rationale below). Each theme (`configs/themes/space.yaml`, `configs/themes/forest.yaml`) already declared a `sounds.hit`/`sounds.miss` path pointing at files that didn't exist; those are now real assets, and `GuiFeedback` (`src/app.py`) now honors `feedback.hit_sound`/`feedback.miss_sound` from each task YAML (closing the exact dead-config gap this item diagnosed) by playing the matching theme cue via `QSoundEffect` on `on_hit`/`on_miss`, at a deliberately reduced volume (0.6) as a sensory-overstimulation safety margin.

- **Scope decision — discrete cues, not background music:** the user's initial framing of this task mentioned "background music," but continuous audio during active trials was explicitly ruled out (user decision, 2026-09-03) as a possible auditory distractor/confound for a visual-attention gaze assessment in a CP/CVI population — there was no clinical-literature basis in this SPEC for adding it, and it would have been new scope beyond the originally diagnosed gap. Scope stayed to the two cues already declared in config.
- **Asset generation:** 4 short (1.2s) WAV cues generated via the user's own `mcp-audio-openrouter` MCP tool (`generate_music`, `google/lyria-3-clip-preview`, OpenRouter) — `space_hit`/`space_miss` (used by `click_grid`/`follow_moving`) and `forest_hit`/`forest_miss` (used by `click_static`/`scanning`). Prompts were designed for the target population specifically, not generically: short, soft, non-startling timbres (bell/chime/xylophone, no drums/bass), a warm ascending two-note phrase for "hit," and a neutral (never punitive/buzzer-like) descending two-note tone for "miss" — an assessment tool's miss cue must not risk discouraging engagement or biasing the child's behavior, which would taint the very accuracy/reaction-time data being collected. User approved the exact prompts before generation.
- **Length-mismatch workaround:** `generate_music`'s shortest preset is a ~30s "clip" (no sub-30s option exists) and the model did not reliably honor a "fade to silence quickly" instruction — all 4 generations played continuously for the full clip. Each was trimmed locally to a clean, short cue via `ffmpeg` (bundled through the pip-installable `imageio-ffmpeg`, run in an ephemeral `uv run --with imageio-ffmpeg` environment — not an app dependency) using a `silencedetect`-then-hard-cap approach, saved as `configs/assets/sounds/{space,forest}_{hit,miss}.wav`, 1.2s each. Raw 30s originals remain in the audio tool's own (gitignored) `output/` dir for reference/re-trimming without paying for regeneration.
- **Diagnosed but NOT resolved in this session — `QSoundEffect` reports `Status.Error` for every WAV tried, in this session's environment specifically.** Live qt-mcp testing surfaced a Qt warning ("playback of this format is not supported on the selected audio device") for both cues on every launch. One real candidate cause was ruled out along the way: ffmpeg's WAV export inserts a `LIST/INFO` metadata chunk between the `fmt ` and `data` chunks, which some minimal WAV parsers reject — final assets are re-exported without it (`-map_metadata -1 -fflags +bitexact`, canonical 16-bit PCM stereo 48kHz header verified byte-for-byte), but the warning persisted unchanged afterward, so it was not the (sole) cause. Conclusively isolated with a control test: a completely stock Windows system sound (`C:\Windows\Media\chimes.wav`), loaded via a minimal standalone `QSoundEffect` script, fails identically (`Status.Error`, `isLoaded: False`) — proving this is an audio-output-device limitation of the automated session this work ran in, not a defect in the generated files or the app's wiring code.
- **Verification caveat — could not be audibly confirmed by the implementing session, for the reason above.** The generated files are independently confirmed structurally valid: Python's `wave` module reads all 4 cleanly (correct channel count/sample width/rate/duration), and `System.Media.SoundPlayer.PlaySync()` — a separate, non-Qt Windows audio API — played `space_hit.wav` successfully. The app itself never crashes regardless of the audio-device failure (`QSoundEffect.play()` on an errored source is a safe no-op) — verified across three live qt-mcp GUI launches with no exception. **The user should listen to the 4 files themselves** (`configs/assets/sounds/*.wav`) on a machine with a working, bound audio output device and confirm both that they play at all and that they sound as intended, before considering this item fully done; regeneration/re-prompting is one MCP tool call away if not.
- **Tests:** `tests/test_theme_sounds.py` (new) — asserts each theme declares hit/miss sounds and that the files exist and are short (≤2s), valid 16-bit PCM WAV. Kept Qt-free, matching this project's existing convention of not unit-testing PySide6 GUI code directly (verified live via qt-mcp instead, as with item 3).

---

## 5. Are target speed and inter-trial pacing configurable?

**Observed:** wants to know if the follow_along circle can move slower, and if there's more pause between circles in the other tasks.

**Investigated — yes, both already exist as config, just not surfaced/documented for a non-engineer:**
- **follow_along movement speed:** `motion.speed_frac_per_s` in `configs/tasks/follow_moving.yaml`, currently `0.20` (fraction of screen width per second the target moves). Lower value = slower motion. A related setting, `motion.select_window_ms` (currently 2500ms), controls how long the moving target stays selectable/highlighted.
- **Inter-trial pacing, all four tasks:** `inter_trial_interval_ms` in each task's YAML controls the pause between one trial ending and the next target appearing — this is the "time between circles" being asked about. Current values: `click_static` 800ms, `click_grid` 800ms, `scanning` 900ms, `follow_moving` 1000ms.
- There is no separate "appear" animation — the next target simply appears the instant the inter-trial interval elapses; the pause itself is the only adjustable "slowness" between targets.
- Each task also has its own independently configurable `timeout_ms` (how long a trial waits before giving up), in case that overlaps with what "slower" was meant to address.

---

## 6. Meaning of recorded/analysis data fields, and what's clinically relevant for CP

**Observed:** physician asked what the saved data fields mean; user couldn't answer. Also: what GP3HD data is actually relevant for assessing Cerebral Palsy?

**Investigated — fields currently written to disk per session (`sessions/<session_id>/`):**

`trials.csv` — one row per completed trial:

| Field | Meaning |
|---|---|
| `trial_id`, `task_id` | trial index, task name |
| `target_x`, `target_y` | normalized (0-1) target position |
| `target_radius_px` | target hitbox radius, pixels |
| `t_target_shown_ns` | timestamp the target appeared |
| `t_first_gaze_on_target_ns` | timestamp gaze *first* entered the target hitbox (first-fixation time) |
| `t_click_ns` | timestamp of the successful dwell-selection |
| `t_end_ns` | timestamp the trial ended (hit or timeout) |
| `is_hit` / `is_timeout` | outcome flags |
| `attempts` | number of selection attempts in the trial (off-target attempts count too) |
| `reaction_time_ms` | `t_click_ns − t_target_shown_ns` |
| `time_to_first_fixation_ms` | `t_first_gaze_on_target_ns − t_target_shown_ns` |

`gaze_stream.csv` — raw per-sample gaze log (if `recording.save_gaze_stream` is on): `t_ns`, `x`/`y` (normalized point of gaze), `valid`, `fixation_id`, `fix_duration_s`, `pupil_left`/`pupil_right` (mm).

`metadata.json` — subject/session IDs, calibration point count, mean calibration error in px, input mode, task list.

`events.jsonl` — the raw event log (`TARGET_SHOWN`, `MISS_CLICK`, `HIT`, `TIMEOUT`, `LATENCY_SAMPLE`, ...) that the above are derived from.

**What `analysis/analyze_session.py` currently computes:** hit rate, timeout count, mean reaction time, a reaction-time histogram, and a 2D gaze heatmap. Nothing pupil- or fixation-specific is summarized today, even though `pupil_left`/`pupil_right`/`fixation_id`/`fix_duration_s` are already recorded in the raw stream.

**What's clinically relevant from a GP3HD-class tracker for CP assessment (external research):**

- **Fixation & saccade latency, frequency, and "off-screen" proportion.** A 2025 study (*Ophthalmology Science*/ScienceDirect) found six such features together reached AUC ≥0.90 distinguishing children with cerebral/cortical visual impairment (common in CP) from age-matched controls, and tracked them longitudinally as an objective outcome measure. This tool already records the raw ingredients for these (`fixation_id`, `fix_duration_s`, per-trial timestamps) but doesn't yet aggregate them into session-level features.
- **Time-on-task and selection accuracy.** The metrics used in a published longitudinal study of gaze-based AT (using the same "Compass" software `click_grid` is explicitly modeled on, per its own code docstring) that tracked real functional improvement in children with CP over months of use (PMC4867850). This tool's `reaction_time_ms`/`is_hit`/`attempts` already map onto this directly.
- **Single-target fixation vs. target-target fixation shift.** A 2025 study (PubMed 40217776) assessing exactly these two skills in children with CP found meaningful child-to-child variability between them — reinforcing why visually and clinically distinguishing `click_static` (fixation) from `scanning`/`click_grid` (shift/search) matters (ties back to item 3's finding that they're currently indistinguishable).
- **Pupil diameter.** Already captured (`LPMM`/`RPMM` via the GP3HD) but never summarized. In the wider literature this is used mainly as a cognitive-load/attention/arousal signal rather than a CP-specific oculomotor marker — likely lower priority than the fixation/saccade features above, unless the clinical goal explicitly includes attention/engagement tracking.
- **Calibration accuracy per subject.** Already recorded (`calibration_error_px`) and clinically meaningful in its own right, not just as tooling QA: the 2025 CP fixation study reported 6 of 39 children were unable to calibrate to the required accuracy at all — calibration success/quality is itself a data point about a child's oculomotor control.

**Partially implemented 2026-09-03:** `src/data/exporter.py` gained `load_gaze_rows`, `compute_fixation_saccade_metrics` (session-level fixation count/rate/duration mean-median-max, saccade count/rate, valid-gaze ratio, mean pupil L/R mm — all aggregated from the already-recorded `gaze_stream.csv`, no new tracker data needed), and `compute_trial_fixation_counts` (distinct fixation count per trial window — the single-target-fixation vs. target-target-fixation-shift signal called out above). Wired into `analysis/analyze_session.py`, which now also writes a `session_metrics.json` into the session directory. Ran successfully against the real recorded session at `dev/sessions/saved_session_data` (a `scanning` replay: 114 fixations, 0.30s mean duration, 3.34mm mean pupil). Tests added to `tests/test_recorder.py`. **Not yet done:** turning this into a CVI-style session report/dashboard, or extending it to the AUC-relevant off-screen-proportion / saccade-latency features beyond what's listed here — this is the raw metrics aggregation layer only, open item 4 below is still open.

---

## 7. Calibration re-runs on every task launch, even for the same child

**Observed:** not one of the original six physician feedback items — surfaced 2026-09-03 while drafting a report summary, from the user's own question: "does the same subject need to calibrate again for a different task?"

**Investigated:**
- `src/main.py`'s CLI only accepts a single `--task` per invocation (`argparse` `choices=sorted(TASK_REGISTRY)`), so running all four tasks for one child means launching the app four separate times as four separate processes.
- `AssessmentApp.__init__` (`src/app.py`) runs `Calibration(self.client, ...).run()` fresh on every launch, gated only by `calibration.enabled` in config — there is no session-level state that persists across process launches, so a fresh calibration happens each time regardless of whether the same child sat through one moments earlier.
- **Not a bug** — this is simply how the app is structured today (one process = one task = one calibration). No code currently supports "calibrate once, then run task A, then task B without recalibrating."

**Why this likely matters:** eye-tracker calibration accuracy is generally expected to stay valid as long as head position/distance from the tracker hasn't changed meaningfully — re-running full calibration between tasks in the same sitting adds repeated setup time and burden for children who may fatigue or lose patience easily, without a clear accuracy benefit if the child hasn't moved.

**Design decided 2026-09-04 (user's own call, not yet implemented) — supersedes the multi-task-CLI framing above.** The original diagnosis assumed the fix required redesigning `src/main.py`'s CLI to run several tasks in one process (since one process = one task = one calibration today). The user instead wants a much smaller, per-launch mechanism: a new CLI input that lets *this* launch skip calibration by reusing a previously-recorded one, while each launch otherwise stays exactly as it is today (one process, one task). This also fits the user's stated current priority: finish backend/data-pipeline work first, GUI work comes later — this design adds no GUI surface at all, only a CLI flag and a small file format.

**Sequencing (decided 2026-09-04):** this item was broken into two separately-scoped goals, tackled one at a time —
1. **Goal 1 (current focus):** feasibility/design of reusing a saved calibration file to skip recalibration — the `--calibration-file` mechanism below. This is where implementation will start once this SPEC settles.
2. **Goal 2 (deferred until Goal 1 is settled):** feasibility of adjusting the calibration *point count* (e.g. fewer than 5 points), raised by the physician. Investigated at the corpus level this session — see the dedicated subsection below — but **not designed yet**; picked up only after Goal 1.

The rest of this section documents Goal 1's design. Goal 2's findings are kept separate, below, to avoid conflating two different mechanisms that both touch calibration but solve different problems (skipping calibration entirely vs. making a *performed* calibration faster).

- **Mechanism:** a new `--calibration-file PATH` CLI argument (optional).
  - **Given:** `AssessmentApp` skips sending any `CALIBRATE_*` command to the device this run (no `CALIBRATE_START`, no on-screen dance) and instead loads a previously-saved calibration record from `PATH` to populate `SessionMetadata.calibration_points`/`calibration_error_px` for this session.
  - **Not given:** current behavior, unchanged — `Calibration.run()` executes normally.
- **What "skip calibration" can technically mean, and what it can't:** checked against this project's own verified vendor corpus (`docs/gazepoints/synthesis/api-reference.md`) — the documented `CALIBRATE_*` command set (`START`/`SHOW`/`TIMEOUT`/`DELAY`/`RESULT_SUMMARY`/`CLEAR`/`RESET`/`ADDPOINT`) has **no command to upload or restore calibration coefficients**. So a "calibration file" cannot be fed back into the Gazepoint tracker itself — it is this app's own bookkeeping record. Skipping calibration means *not issuing `CALIBRATE_START` this run* and trusting whatever calibration Gazepoint Control (a separate, continuously-running application, not restarted per client connection) currently has active from a prior run.
  - **Open technical risk, unverified — needs the live device:** the assumption that Gazepoint Control's calibration state survives our client disconnecting and a new client reconnecting later is architecturally reasonable (calibration lives in Gazepoint Control's own process, not in the transient OpenGaze socket) but is **not stated anywhere in the verified corpus** and has not been tested live. Before relying on this for real sessions, do a live check: calibrate once, fully disconnect/stop this app, relaunch with `--calibration-file` pointing at that run's saved record, and confirm gaze accuracy is still good (e.g. via a quick `click_static` run) rather than degraded/uncalibrated.
- **File creation (user-chosen 2026-09-04): automatic, no separate save flag.** Whenever `Calibration.run()` actually executes a real calibration (not the existing `enabled: false`/no-hardware stub path), the app automatically writes the result to a file. Proposed location: `<session_dir>/calibration.json` (alongside `metadata.json`/`trials.csv`/etc., reusing the existing per-session output convention rather than inventing a separate calibration store) — to skip calibration on a later launch, point `--calibration-file` at a previous session's saved `calibration.json`.
  - Proposed fields: `subject_id`, `n_points`, `mean_error_px`, `valid`, `calibrated_at` (ISO timestamp).
- **Safety check (user-chosen 2026-09-04): cross-check `subject_id`.** If `--calibration-file`'s `subject_id` doesn't match this run's `--subject`, refuse to reuse it and error out (fail fast) rather than silently proceeding with a mismatched or default-fallback calibration — this guards against accidentally pointing at the wrong file (currently low-risk with a single user, but cheap to build in now and necessary once this is used with real children).
- **Goal 1 — still open / not decided (implementation-time questions):**
  1. Staleness: should there be any warning/cutoff based on `calibrated_at` age (e.g. "this calibration is from 3 hours ago, re-run?"), or is any age acceptable as long as `subject_id` matches and the physical setup hasn't changed? Not decided.
  2. Malformed/missing file behavior: proposed default is a hard error telling the user to fix the path or omit the flag (rather than silently falling back to full calibration) — not yet confirmed.
  3. Interaction with the existing `calibration.enabled: false` config toggle (today's stub-mode skip, which records `mean_error_px: None`): these are two different "skip" mechanisms with different intent (stub/dev-mode vs. reusing a real prior measurement) — precedence between them if both are set at once is not yet decided.
  4. `Calibration` class shape: likely needs a new constructor path (e.g. an optional pre-supplied `CalibrationResult`) so `run()` can return the reused record immediately without touching the socket, rather than overloading the existing `enabled=False` stub path (which currently always reports `mean_error_px=None`, not a real reused value).

**Goal 1 implemented 2026-09-04.** All four open implementation-time questions above are now resolved (the first three per the user's explicit decision, the fourth by the implementation itself):

1. **Staleness:** no cutoff — any age is accepted as long as `subject_id` matches. `calibrated_at` is still recorded in the file for the operator's own reference, just not enforced.
2. **Malformed/missing file:** hard error, as proposed — no silent fallback to a fresh calibration.
3. **Interaction with `calibration.enabled: false`:** `--calibration-file` wins. If both are set, the reused real calibration is used regardless of the stub toggle.
4. **`Calibration` class shape:** a new `preset_result: CalibrationResult | None` constructor parameter (`src/engine/calibration.py`). When given, `run()` returns it immediately without touching the socket or validating `n_points` against `_CALIBRATION_LAYOUTS` (a reused record isn't limited to the vendor-supported live point counts).

**What was built:**
- `src/engine/calibration.py`: `CalibrationFileError` (raised on any missing/malformed/mismatched calibration file — no silent fallback), `SavedCalibration`, `save_calibration_result(path, subject_id, result)`, `load_calibration_result(path)`; `Calibration.__init__` gained `preset_result`; `Calibration.is_stub` property (mirrors `run()`'s stub condition: no socket, or `enabled=False`) so callers know whether a completed run is worth auto-saving.
- `src/app.py` (`AssessmentApp.__init__`): new `calibration_file: str | None` parameter. Session id/dir are now computed up front (moved earlier than before) so an auto-saved `calibration.json` can be written into the session dir *before* the rest of session setup. If `calibration_file` is given, it's loaded and `subject_id`-checked **before the device client is even constructed** — a bad path or mismatch fails fast with no socket opened and no calibration UI shown. If not given, a normal calibration runs as before, and is auto-saved to `<session_dir>/calibration.json` iff `not calibration.is_stub` (i.e. a real device calibration actually ran) — a replay/no-device run never fabricates a fake calibration record. `run_gui` catches `CalibrationFileError` and exits with code 2 and a clear stderr message, matching the existing pattern for missing GUI deps.
- `src/main.py`: new `--calibration-file PATH` CLI flag, GUI-only (a stderr note is printed and the flag ignored if passed without `--gui`, since headless replay never calibrates at all).
- **Tests:** 11 new tests in `tests/test_calibration.py` — `preset_result` short-circuits `run()` without sending any bytes to the device (verified against a real listening socket that would otherwise reveal any command sent), works with no client at all, and bypasses `n_points` validation; `is_stub` true/false across no-socket/disabled/real-socket-enabled; `save_calibration_result`/`load_calibration_result` round-trip including a `None` `mean_error_px`; and `CalibrationFileError` for a missing file, malformed JSON, and missing/invalid fields.
- **Functional (non-pytest) verification**, offscreen Qt against the replay fixture (no real device): constructing `AssessmentApp` with a matching-`subject_id` `--calibration-file` correctly adopts the preset calibration's `n_points`/`mean_error_px` into `SessionMetadata`; a mismatched `subject_id` and a missing file both raise `CalibrationFileError` before any client/session side effects; a normal replay run (device `_sock` always `None`) does not auto-save a `calibration.json`.
- **Not yet verified — needs the live device**, per the open technical risk flagged in the design above: whether Gazepoint Control's calibration state actually survives this app's client disconnecting and a later client reconnecting. Do a live check before trusting this for real sessions: calibrate once, fully stop the app, relaunch with `--calibration-file` pointing at that run's saved `calibration.json`, and confirm gaze accuracy is still good (e.g. via a quick `click_static` run).
- **Regression note:** two pre-existing, unrelated test failures remain (confirmed via `git stash` to predate this session's changes) — `test_config_merges_task_over_default` (the known stale `target_fps` assertion, see the 2026-09-04 update above) and `test_click_static_records_hits` (a headless-replay hit-count assertion, newly noticed this session, not investigated — flagged here for a future session rather than silently left undocumented).
- **Deliberately left uncommitted** as of this write-up — the user was offered a commit+push (matching how items 1-6 shipped) and explicitly chose to leave it as working-tree changes for now. Not a sign of incompleteness; see the memory-system note below if this SPEC is being read from a future session.

**Device-free testing tooling added 2026-09-04 (same day, follow-up):** the automated tests and offscreen functional script above cover the code paths, but don't exercise the real `python -m src.main --gui` CLI entrypoint end to end, and can't demonstrate the *auto-save* half (which needs a real socket, never opened by `--replay`). Two gaps closed:
- `tools/fake_gazepoint_server.py` (new) — a minimal TCP server standing in for Gazepoint Control: accepts a connection the same way, ACKs `CALIBRATE_RESULT_SUMMARY` queries with a fixed valid result so `Calibration.run()` completes, and otherwise sends no `REC` gaze data (it's for exercising the connect/calibrate/auto-save handshake only, not a moving-gaze substitute — use `--replay` for that). Placed in `tools/` alongside the existing `make_replay_fixture.py`, matching this project's convention for standalone dev utilities.
- `README.md` gained a new "Testing `--calibration-file` without a device" section documenting both the plain-reuse case (no server needed, just `--replay` + a hand-written `calibration.json`) and the full auto-save+reuse loop against the fake server (temporarily pointing `configs/default.yaml`'s `gazepoint.host` at `127.0.0.1`, per the existing skip-worktree convention).
- **Verified twice, independently:** first by this session directly (constructing `AssessmentApp` against the fake server offline, confirming `calibration.json` was written with the correct fields, then a second launch reusing it), and separately confirmed working by the user themselves running the actual `python -m src.main --gui` command against the fake server on their own machine — "the command work[s] without error." This is a stronger confirmation than the pytest-level and offscreen-script-level checks alone, since it exercises the real CLI entrypoint, the real Qt event loop, and a real (if fake-server-backed) TCP connection, not a Python-level shortcut.
- **Note for future sessions:** `configs/default.yaml`'s `gazepoint.host` is currently left at `"127.0.0.1"` (with an inline comment recording both addresses) rather than reverted to the real device's `26.113.49.235` — this is a deliberate, skip-worktree'd local edit the user made to keep testing against the fake server, not a mistake to silently "fix." Confirm with the user before switching it back to the real device address.

### Goal 2: can the calibration point count be adjusted, e.g. fewer than 5?

**Observed:** raised by the physician (via the user) on 2026-09-04, framed as "if the same subject wants to do the task the next day, can they skip straight to the task instead of recalibrating?" — the user's own assumption at the time was that the device could not do fewer than 5 points; re-checked against the corpus per the user's explicit request.

**Investigated — the "device can't do that" assumption is only partly right:**
- The vendor's formal hardware spec table (`docs/gazepoints/sources/gazepoint-control.md`, spec row) lists calibration as **"5 or 9 point"** for both GP3 and GP3HD — that is the officially spec'd mode, matching the user's original assumption.
- However, `CALIBRATE_ADDPOINT` (`docs/gazepoints/sources/gazepoint-api.md` §3.10) is a general-purpose API command with **no documented minimum (or maximum) point count** — a custom point list of any size is built via `CALIBRATE_CLEAR` + one-or-more `CALIBRATE_ADDPOINT` calls + `CALIBRATE_START`. Nothing in the wire protocol itself blocks fewer than 5.
- Gazepoint Control's own UI (§4.3, same doc) exposes a dedicated **1-point calibration mode** (press `1` in the calibration window), confirming the vendor itself considers sub-5-point calibration a real, intentional feature — not just an API technicality.
- **Important nuance, corrected after a first pass that overstated it:** the 1-point mode's exact documented behavior is *"uses a default set of values for calibration and does a minor correction with the 1 point... may be VERY inaccurate depending on how different the system setup and subject are from the default calibration."* This nudges a **generic/factory baseline**, not the specific child's own previously-recorded calibration — so it is not a mechanism for "cheaply revalidating yesterday's good calibration for this subject." It is closer to a fast/rough calibration mode traded off against accuracy.

**Re-investigated 2026-09-04 (memory-restore session, later the same day) — the "does raw ADDPOINT reproduce Control's special 1-point algorithm" question is now resolved, not just newly nuanced:**

- Re-read `gazepoint-api.md` §3.8-§3.10 closely, including its own worked example (not just the command signatures) — `CALIBRATE_ADDPOINT`'s documented example starts from a 5-point list (`PTS="5"`) and a single `SET ID="CALIBRATE_ADDPOINT" X="0.5" Y="0.1"` call grows it to `PTS="6"`. **This is the vendor's own manual demonstrating an arbitrary, non-5/9 point count (6) via the API**, not something this session inferred — stronger evidence than before that the API itself has no point-count restriction at all.
- **Key new finding: there is no API command corresponding to Control's `1`/`5`/`9` keyboard shortcuts.** `setup-and-calibration.md` and `gazepoint-control.md` §4.3 both describe `1`/`5`/`9`/`C`/`U`/`S`/`+`/`-`/`D`/`m` purely as **keys pressed inside Control's own rendered calibration window** — a local UI interaction, not a documented `SET`/`GET` ID anywhere in the 1100+-line API manual (confirmed via a full-text search of `docs/gazepoints/` for the calibration command IDs — `CALIBRATE_START`/`SHOW`/`TIMEOUT`/`DELAY`/`RESULT_SUMMARY`/`CLEAR`/`RESET`/`ADDPOINT` is the complete list, nothing else). **Conclusion: this app's headless TCP client cannot reach Control's special "default-values + minor correction" 1-point algorithm at all, under any circumstances** — not "unverified whether it's reproduced," but confirmed there is no API surface for it. Any point count our client sends goes through the one and only mechanism Control exposes over the wire: a plain point list plus the standard calibration regression.
- **Practical consequence (reasoned from this, not directly stated in the docs — still needs a live check to confirm actual accuracy numbers):** sending very few points (especially 1-2) via raw `CALIBRATE_ADDPOINT` is expected to produce a poorly-fit or degenerate calibration, because it would run the same regression-based mapping the 5/9-point modes use, just underdetermined — which is presumably *why* Control needs a wholly separate "nudge a default baseline" algorithm for its own 1-point UI mode in the first place, rather than just calling its regular calibration routine with 1 point. This isn't a protocol limitation (nothing stops sending 1 or 2 `ADDPOINT`s), it's an expected numerical/accuracy one.
- Our own `Calibration` class (`src/engine/calibration.py`) currently hardcodes `_CALIBRATION_LAYOUTS` to only `{5, 9}` and raises `ValueError` for anything else — so today's codebase doesn't expose *any* other point count, even though the API itself would accept one.

**Feasibility conclusion: yes, specifying an arbitrary point count in calibration IS possible** at the protocol level (any n ≥ 1, confirmed from the vendor's own documented example) — implementing it would need:
1. Loosening `Calibration.__init__`'s `n_points not in {5, 9}` check to something like `n_points >= 1` (or a small sane minimum/maximum this project chooses, since the vendor doesn't impose one).
2. A point-layout generator for point counts the code doesn't already hand-craft — `_CALIBRATION_LAYOUTS` today only has the 5-point (vendor default, via `CALIBRATE_RESET`) and 9-point (this project's own 3×3 extension) layouts baked in as literal coordinate lists; anything else (2, 3, 4, 6, 7, 8, 10+) needs either more hand-picked layouts or a formula (e.g. an evenly-spaced grid scaled to n). **This is a design choice, not just a technical unlock** — how points should be arranged for an arbitrary n isn't specified anywhere in the vendor docs (only 5 and 9 have documented "official" layouts).
3. Config/CLI wiring: `calibration.points` already exists as a plain int in `configs/default.yaml`, so removing the `{5,9}` restriction alone would let YAML-level experimentation happen immediately; a CLI override (mirroring `--calibration-file`'s precedent) would be a small additional convenience, not a requirement.
4. **Separately unresolved, would need the real device**: actual achieved `mean_error_px` at low point counts (2, 3, 4) — the reasoning above predicts it'll be worse than 5-point, but by how much, and whether it crosses into "unusable for this population," can't be answered from documentation alone.

**Still not decided (this project's own call, not a vendor constraint) — a design/scope question for the user before implementing:**
- Whether to build this at all now, given the practical accuracy concern just reasoned through above, versus treating the physician's actual underlying need ("can we skip re-calibrating tomorrow if the child hasn't moved") as already answered by Goal 1's `--calibration-file` mechanism instead.
- If built: what point counts to support (any positive integer? a curated set like `{2, 3, 4, 5, 9}`?), and how to generate layouts for counts beyond the two already hand-crafted.
- How a low-point-count calibration's result should interact with `--calibration-file` — e.g. should a known-lower-accuracy calibration still be eligible to auto-save/reuse the same way a 5/9-point one is, or should `SavedCalibration`/`calibration.json` record the point count so a future session (or a future staleness/quality policy) can distinguish them? (`n_points` is already a field in the saved file today, so this is at least visible, just not yet acted on.)

**Implemented 2026-09-04 (same day, immediately after the feasibility write-up above).** The user's explicit call: build the raw capability now (even with the accuracy tradeoff unmeasured) specifically so a later live-device session can measure that tradeoff and report it to the physician with real numbers, rather than waiting for a policy decision first.

- `Calibration._CALIBRATION_LAYOUTS` (a `{5: [...], 9: [...]}` dict) replaced with a single ordered `_CALIBRATION_POINT_POOL` (9 points: center, then the 4 corners, then the 4 edge midpoints — the same coordinates as before, just unified into one list) and a `_layout_for(n_points)` helper that returns the pool's first `n_points` entries. This reproduces the existing 5-point (`POOL[:5]`) and 9-point (`POOL[:9]`) layouts **exactly**, so nothing already validated (including the live 5pt/9pt calibration confirmed back on 2026-08-31) changes — verified by keeping `_configure_points`'s `n_points == 5` branch sending `CALIBRATE_RESET` unchanged, and confirming `POOL[:9]` is byte-identical to the old 9-point list.
- `Calibration.__init__`'s validation changed from "must be exactly 5 or 9" to "must be 1-9" (`_layout_for` raises `ValueError` outside that range) — chosen as the scope boundary because 9 is where vendor/project precedent ends; no layout is defined above 9, deliberately (not asked for, and no established pattern to follow).
- No CLI flag added — `configs/default.yaml`'s existing `calibration.points` field (already a plain int) now accepts 1-9 directly, so testing a new point count is just a config edit (same skip-worktree-friendly workflow already used for other calibration settings), matching "do what's asked, nothing more."
- **Tests:** `tests/test_calibration.py` gained `test_zero_points_raises`, `test_more_than_nine_points_raises`, a parametrized test across n=1,2,3,4,6,7,8 confirming each sends `CALIBRATE_CLEAR` + exactly n `CALIBRATE_ADDPOINT` commands (never `CALIBRATE_RESET`), plus two exact-coordinate checks (`n=1` is just the center point; `n=4` is center + the first 3 corners, *not* 4 corners without center — the pool is center-first). The two tests that assumed the old `{5,9}`-only restriction (`test_invalid_n_points_raises` with `n_points=7`, and the `preset_result` bypass test) were updated, since 7 is now a valid count on its own — 34 tests total in this file, all passing.
- **Functional verification against the fake server** (`tools/fake_gazepoint_server.py`, same one built for Goal 1): constructed `Calibration(client, n_points=3)` against a real socket and confirmed it completes and returns `CalibrationResult(n_points=3, valid=True, ...)` — previously this would have raised `ValueError` before ever touching the socket. (Two stray fake-server processes from an earlier verification were found still listening on port 4242 during this check — `TaskStop` on this session's tracked background-task wrapper had not actually killed the detached Windows child process; cleaned up via `taskkill /F /T` on the actual PIDs found through `netstat`. Worth remembering for any future session's own fake-server cleanup on Windows: verify with `netstat -ano | grep 4242` after stopping, don't assume `TaskStop` reached the child.)
- **Not done / explicitly out of scope this pass:** no live-device accuracy measurement (that's the user's own next step, the whole reason this was built now); no layout beyond 9 points; no policy decision about how a low-point calibration should interact with `--calibration-file` (both still work together mechanically — `n_points` already round-trips through `calibration.json` — but no staleness/quality distinction was added, per the open Goal 2 questions above, none of which were resolved by this implementation pass, only unblocked for the user to explore).

---

**Shipped 2026-09-04:** items 1, 2, and 5's implementation (plus the
`_draw_progress_ring`/`_draw_target`/`_draw_instant_feedback` `QPen` fix
above) is committed on `main` at `663fac4` ("Implement physician-feedback
items 1, 2, 5: gaze smoothing, instant on-target ring, pacing docs") and
pushed to `origin/main`. Combined with items 3, 4, 6 from `e1a3bbe`
(2026-09-03), everything in this document through item 6 is on
`origin/main` as of that commit. `configs/default.yaml`'s `git skip-worktree`
flag (see `peds-eye-gaze-assessment-config-skip-worktree-2026-09-03` memory /
README.md) was re-applied after that commit — a future session should still
check `git ls-files -v | grep '^S'` before trusting `git status` on that file.

**Item 7 status (updated, later the same day) — both goals implemented but
NOT committed.** Unlike items 1-6 above, item 7's full implementation (Goal
1's `--calibration-file` mechanism AND Goal 2's 1-9 point calibration) is
complete and verified — including by the user's own hands-on run of the
real CLI for Goal 1 — but exists only as working-tree changes in
`dev/peds-eye-gaze-assessment`: `src/app.py`, `src/engine/calibration.py`,
`src/main.py`, `tests/test_calibration.py`, `tools/fake_gazepoint_server.py`
(new/untracked), `README.md`, and this SPEC file. The user was explicitly
asked whether to commit+push (matching how items 1-6 closed out) and chose
to leave it uncommitted for now. **How to apply:** if a future session runs
`git status`/`git log` and sees item 7's code present but not on `main`,
that's expected — don't assume it's abandoned or unfinished work from
another session; ask the user about committing rather than discarding or
silently committing it yourself.

## Open items for the next PI/physician conversation

1. **Items 1 & 2** are almost certainly the same root cause (unsmoothed raw gaze signal interacting with an 800ms/120ms dwell timing model) — confirm whether the complaint is the jitter itself, the missed selections, or both, to prioritize correctly. ~~Both fixed 2026-09-04 (gaze smoothing + instant on-target ring, see above), tested against a synthetic jitter pattern.~~ Still open: the fix has not been tuned or validated against the *real* GP3HD's actual noise characteristics — only synthetic square-wave jitter (no device was connected this session).
2. **Item 3/6:** ~~since `click_grid` and `scanning` don't currently render their intended multi-item layouts, decide whether to (a) build the missing visuals so the tasks are clinically valid as designed, or (b) reconsider whether three separate "click" tasks are still needed if they currently test the same thing.~~ Part (a) is now done (2026-09-03, see item 3 above) — the visuals are built. Still open: now that `click_grid`/`scanning`/`click_static` look visually distinct, does the PI/physician still want to reconsider consolidating them, or is the current three-task design confirmed?
3. **Item 4:** ~~what's the actual plan for sound assets — institution-licensed clips, royalty-free/CC0, or AI-generated?~~ Answered — AI-generated via `mcp-audio-openrouter`, implemented 2026-09-03. Still open: the user needs to listen-confirm the 4 files actually sound right (see item 4 above), and, separately, whether background/ambient music is wanted for non-assessment contexts (deliberately deferred, not built).
4. **Item 6:** ~~should `analyze_session.py` be extended to surface fixation/saccade-derived features (per the CVI-classification literature above) as a priority, since the raw data needed already exists?~~ Done for the raw aggregation layer (2026-09-03, see item 6 above). Still open: should this be extended further into a formal CVI-classification-style session report, and should it be run automatically as part of every session close rather than as a manual `analyze_session.py` invocation? (Now tracked in more detail, including a full session-level metrics table, in `CLINICAL_DATA_REFERENCE.md`.)
5. **Item 7:** ~~confirm before implementing — does the PI/physician want calibration re-validated between tasks anyway as a data-quality safeguard, or is calibrate-once-per-sitting acceptable?~~ **Both goals implemented 2026-09-04.** Goal 1 (`--calibration-file`, see item 7 above) accommodates either policy, since re-validating is simply "don't pass the flag." Goal 2 (`calibration.points` now 1-9, was locked to 5/9) shipped the same day at the user's explicit request, specifically to enable live-device accuracy measurement later rather than to answer a policy question now. Still open: the live-device check of whether Gazepoint Control's calibration survives a client reconnect (Goal 1, flagged above); and, once Goal 2's accuracy numbers exist, **the actual question for the PI/physician** — is a faster-but-measurably-less-accurate custom calibration (e.g. 3-point) worth offering, given Goal 1 already solves the same-day no-head-movement case without any accuracy tradeoff at all? See the "Goal 2" subsection under item 7 for the full reasoning and what was and wasn't built.

## Sources consulted

- PubMed 40217776 — *Assessment of Gaze Fixations and Shifts in Children with Cerebral Palsy: A Comparison of Computer- and Object-Based Approaches* (2025)
- ScienceDirect/Ophthalmology Science — *Cerebral/Cortical Visual Impairment Classification and Categorization Using Eye Tracking Measures of Oculomotor Function* (2025)
- PMC4867850 — *Eye gaze performance for children with severe physical impairments using gaze-based assistive technology — A longitudinal study*
- napacenter.org — *5 Visual Scanning and Tracking Activities* (OT distinction between tracking and scanning)
- Wikipedia — *Smooth pursuit*; iMotions — *Eye Movements Explained*
